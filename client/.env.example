# Base URL of the backend API + Socket.IO server. Leave empty during local
# dev (Vite's proxy in vite.config.js forwards /api and /socket.io to
# http://localhost:5000). Set this to your deployed backend URL in production.
VITE_SERVER_URL=
